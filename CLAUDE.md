# 프로젝트: 화장품 수출 B2B 발주 사이트

> 이 문서는 Claude Code가 프로젝트 맥락을 이해하고 이어서 작업하기 위한 안내서입니다.
> 프로젝트 폴더 루트에 `CLAUDE.md` 파일명으로 두면 매 세션 자동으로 참고됩니다.

---

## 0. 가장 먼저 — 작업 방식 (중요)

- 이 프로젝트는 **학습을 겸한 1인 개발**입니다. 코드만 쏟아내지 말고, **무엇을·왜 그렇게 하는지 짧게 설명**하면서 진행해 주세요.
- 개발자는 **숙련된 엔지니어이지만 웹 생태계는 거의 처음**입니다. (배경: C++/Qt·QML on webOS, 앱·서비스 서버 개발, 10년+ AI Agent, 반도체 설계.) 따라서:
  - 프로그래밍 기본 개념은 설명 불필요. **웹 특유의 개념**(브라우저, 번들링, 라우팅, React 상태, 서버/클라이언트 컴포넌트 등)은 처음 등장할 때 한두 줄로 짚어 주세요.
  - 가능하면 **QML/네이티브 앱 경험에 빗대어** 설명하면 이해가 빠릅니다. (예: React state ≈ QML property 바인딩)
- **한 번에 한 단계씩.** 큰 변경은 작은 단위로 쪼개고, 각 단계 끝에 "지금 한 일 / 확인 방법 / 다음 단계"를 알려 주세요.
- 막혔을 때를 대비해 **에러 메시지 읽는 법**과 **롤백 방법**도 같이 알려 주세요.
- 의사결정이 필요한 갈림길에서는 임의로 정하지 말고 **선택지와 추천을 제시**하고 물어봐 주세요.

---

## 1. 프로젝트 개요

**화장품 수출업**을 위한 B2B 발주 웹사이트.

### 현재 상황 (해결하려는 문제)
- 기존 고객을 대상으로 **이메일·메신저로 수작업** 처리 중.
- 흐름: 제품 문의 → 메시지로 가격 안내 → 주문 접수 → 발주 → 송금 받기.
- 비효율적이고 실수·누락이 잦으며, 확장이 안 됨.

### 고객 특성
- 대부분 **해외 B2B 바이어** (인도네시아 등).
- 신규 유입이 아니라 **기존 고객을 사이트로 이주**시키는 것이 1차 목표.

### 목표
사이트에서 (1) 상품·가격 조회, (2) 발주, (3) 문의가 가능하게 만들어
이메일·메신저 핑퐁을 없애고 주문 과정을 디지털화한다.

---

## 2. 핵심 설계 원칙 (이미 합의된 사항)

1. **결제는 1단계에 넣지 않는다.**
   해외 B2B 무역 특성상 금액이 크고 환율·관세·인코텀즈가 얽힘. 온라인 카드결제 모델이 부적합하고, 해외 PG·외화정산은 그 자체로 별도 프로젝트.
   → **송금은 기존처럼 오프라인 유지.** 사이트는 "주문 접수 ~ 견적/인보이스 확정 ~ 송금 안내"까지만 디지털화. (결제 연동은 사업이 사이트로 안정된 후 별도 검토.)

2. **B2B 발주 = 견적 기반 흐름.** 일반 쇼핑몰의 "장바구니→즉시결제"가 아니라:
   고객이 발주서 제출 → 관리자가 확인·견적 확정 → 고객에게 안내. (가격이 고객/물량별로 다를 수 있음을 염두에 둘 것.)

3. **두 종류의 사용자 화면이 필요하다.**
   - **고객용 사이트**: 해외 바이어가 사용.
   - **관리자 화면(어드민)**: 운영자·직원이 사용. **비개발자도 쓸 수 있어야 함.** 코드 수정 없이 상품 등록·가격 변경·주문 확인·문의 답변 가능해야 함.
   - 두 화면은 **같은 데이터(백엔드)를 공유**한다.

4. **다국어.** 고객용 화면은 영어 기본, 이후 인도네시아어 등 확장 고려. 관리자 화면은 한국어. (1단계엔 영어/한국어만으로 시작해도 됨. 문구를 하드코딩하지 말고 처음부터 다국어 구조를 염두에 둘 것.)

5. **웹 먼저, 앱은 나중에.** 향후 React Native로 모바일 앱 확장 예정이므로, 그 전환이 자연스럽도록 로직과 UI를 분리해 둔다.

---

## 3. 기술 스택 (확정)

| 영역 | 선택 | 비고 |
|------|------|------|
| 프론트엔드 | **React + Next.js (App Router)** | 언어는 **TypeScript** |
| 데이터·백엔드 | **Supabase** | DB(PostgreSQL) · 인증 · 파일 저장(상품 이미지) · 자동 API |
| 배포 | **Vercel** | GitHub 연동 자동 배포 |
| 향후 앱 | **React Native** | 같은 언어/사고방식으로 확장 |
| 개발 환경 | **Windows + PowerShell**, VS Code, Node.js LTS, Git/GitHub | |

- 새 라이브러리를 추가할 때는 **왜 필요한지** 먼저 설명할 것. 의존성을 최소로 유지.
- 스타일링은 가벼운 것으로 시작(예: Tailwind CSS). 도입 시 이유 설명.

---

## 4. 데이터 모델 (초안 — 함께 다듬을 것)

- **products**: 상품명, 설명, 카테고리, 이미지(여러 장), SKU/코드, 기준가, 통화, 활성 여부.
- **prices**: (선택) 고객별·물량별 가격이 필요하면 분리. 1단계엔 기준가 1개로 단순화 가능.
- **customers**: 회사명, 담당자, 국가, 연락처, 로그인 계정. (B2B이므로 **관리자 승인/계정 생성** 방식 고려 — 아무나 가입 X.)
- **orders (발주서)**: 고객, 주문 항목들(상품·수량), 상태, 메모, 생성일.
- **order_status 흐름**: `제출됨 → 확인/견적확정 → 인보이스발행 → (오프라인)입금확인 → 배송/완료`.
- **inquiries (문의)**: 고객, 제목, 내용, 상태, 답변 스레드.
- **admin_users**: 운영자·직원 계정 (관리자 권한).

> 인증/권한: **고객(바이어)**과 **관리자** 두 역할(role)을 구분. Supabase Auth + Row Level Security로 데이터 접근을 역할별로 제한.

---

## 5. 개발 로드맵 (단계별 — 이 순서로 진행)

각 단계는 "동작하는 결과물"로 끝나야 하며, 끝날 때마다 Vercel에 배포해 실제로 확인한다.

- **Phase 0 — 첫 배포**
  빈 Next.js 프로젝트 생성 → 화면에 글자 출력 → GitHub 푸시 → Vercel로 실제 인터넷 주소에 배포. (목표: "내가 만든 게 웹에 떴다" 확인.)

- **Phase 1 — 상품 카탈로그 (읽기)**
  Supabase에 products 테이블 생성 → 고객용 상품 목록·상세 페이지 → 관리자 화면에서 상품 등록·수정·이미지 업로드.

- **Phase 2 — 인증/계정**
  관리자 로그인, 고객 로그인. 역할 구분 및 접근 제한(RLS).

- **Phase 3 — 발주(주문)**
  고객이 상품 골라 발주서 작성·제출 → 관리자 화면에서 주문 목록·상세 확인 → 상태 변경(견적 확정 등). 송금 안내 표시.

- **Phase 4 — 문의**
  고객 문의 작성 → 관리자 답변. (이메일 알림은 선택.)

- **Phase 5 — 다국어 · 마무리**
  영어/인도네시아어 등 다국어, 반응형(PC·모바일 웹) 정리, 기본적인 사이트 홍보 준비(SEO, 공유용 메타데이터 등).

- **이후 — 모바일 앱**
  React Native로 고객용 앱. 사이트 홍보/마케팅 본격화.

> 한 번에 한 Phase. 현재 어느 Phase인지 모르면 먼저 물어볼 것.

---

## 6. 지금 시작점

> **이 섹션은 매번 의미 있는 작업이 끝날 때마다 최신 상태로 갱신할 것.** (VSCode를 다시 열었을 때 여기만 봐도 어디까지 됐는지 알 수 있어야 함.)
> 세부 작업 단위·다음 할 일 체크리스트는 **`TODO.md`가 최신 소스**임 — 세션 시작하면 CLAUDE.md로 큰 그림을 파악하고, `TODO.md`에서 "⬜ 런칭까지 할 일"부터 확인할 것.

**현재: 로드맵 Phase 0~5 핵심 기능은 전부 구현 완료, 런칭 준비 단계 (2026-07-06 기준).**

완료된 단계:
- ✅ Phase 0 — 첫 배포 (GitHub `moos2k/nunas-b2b`, Vercel 자동배포)
- ✅ Phase 1 — 상품 카탈로그 → 브랜드+가격표 다운로드 방식으로 발전
- ✅ Phase 2 — 인증/계정 — 관리자·고객 로그인, 가입 신청/승인·거절(상호 전환), RLS
- ✅ Phase 3 — 발주 — 엑셀 업로드 방식(드래그&드롭, 다중 파일), 관리자/고객 ZIP 일괄 다운로드
- ✅ Phase 4 — 문의 — 고객 작성/관리자 답변
- ✅ Phase 5 — 다국어(영/한, next-intl), 반응형, PWA(홈 화면 설치)

로드맵에 없었지만 추가된 것:
- **PWA**: 홈 화면 설치 배너(안드로이드 원터치/iOS 안내), 브랜드 아이콘, 스플래시 화면
- **가입 승인/거절 안내 메일** (네이버 SMTP)
- **커스텀 도메인** 연결: `https://www.jay-on.co.kr`

개발 환경:
- 코드 위치: `C:\dev\nunas-b2b`
- 프로덕션 도메인: **`https://www.jay-on.co.kr`** (구 jayon.vercel.app, 그 전엔 nunas-b2b.vercel.app — 도메인 언급 시 항상 최신 값 확인할 것)
- 이 PC는 한 번 새로 세팅되어 Node.js가 없었던 적 있음 (winget으로 설치, PATH 이슈는 세션마다 재확인 필요할 수 있음)

**남은 런칭 체크리스트** (자세한 건 `TODO.md` 참고):
- 사이트명 확정 (Jay-On / J.ON 혼용 정리)
- 오픈일 결정
- 누나 실사용 피드백 수집 후 반영

세션을 시작하면:
1. 이 섹션과 `TODO.md`로 마지막으로 어디까지 했는지 확인하고,
2. 남은 작업을 작은 단위로 쪼개 제안한 뒤,
3. 하나씩 진행하며 확인 방법을 알려주고, 끝나면 이 섹션도 갱신할 것.
